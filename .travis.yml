language: java

dist: xenial
jdk: openjdk11
install: true

before_install:
  - nvm install node
  - nvm use node
  - npm install -g @angular/cli

script:
  - ./gradlew ciTest --scan --no-daemon -s

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/